Support for Python 3.15
-----------------------

Support for Python 3.15 is added. This includes updates for bytecode
changes such as ``GET_ITER`` / ``POP_ITER`` stack behavior,
``LOAD_COMMON_CONSTANT``, and exception-table splits from ``NOT_TAKEN``.
