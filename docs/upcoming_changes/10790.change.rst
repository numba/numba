Make jump-if-None predicate register names consistent
-----------------------------------------------------

The internal IR temporary name used for the branch predicate generated by
Python 3.11+'s fused ``is None``/``is not None`` jump opcodes now matches
the naming convention used elsewhere in the interpreter for equivalent
predicates. This is an internal, non-user-facing consistency fix.
