Fix  ``np.median`` raises AssertionError for empty arrays when ``NumPy`` returns NaN
------------------------------------------------------------------------------------

Fixed an issue where empty arrays would cause an error. Now empty arrays return NaN as expected.
